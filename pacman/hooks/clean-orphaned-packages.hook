[Trigger]
Operation = Remove
Type = Package
Target = *

[Action]
Description = Checking for orphaned packages...
When = PostTransaction
Exec = /usr/bin/bash -c 'orphans=$(pacman -Qdtq); if [[ -n "$orphans" ]]; then echo -e "⚠️  Orphaned packages:\n$orphans\n\n💡 Run this to remove them:\nsudo pacman -Rns \$\(pacman -Qdtq\)"; else echo "✅ No orphans to remove."; fi'
